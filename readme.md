# AniLog - 애니메이션 리뷰 사이트

## 📁 프로젝트 구조

```
anilog/
├── index.html          # 프론트엔드 (빈 컨테이너)
├── style.css           # 스타일시트 (변경 없음)
├── script.js           # fetch 기반 DOM 생성
│
└── backend/
    ├── server.js       # Express API 서버
    ├── database.js     # SQLite 설정 + 샘플 데이터
    └── package.json    # 의존성
```

## 🔄 데이터 흐름

```
┌─────────┐    ┌─────────────┐    ┌──────────┐    ┌───────────┐
│ SQLite  │ →  │ Express API │ →  │  fetch   │ →  │ DOM 생성  │
│   DB    │    │  /api/anime │    │ (JS)     │    │ (카드)    │
└─────────┘    └─────────────┘    └──────────┘    └───────────┘
```

## 🚀 실행 방법

### 1. 백엔드 설정

```bash
cd backend
npm install
npm start
```

서버가 `http://localhost:3000`에서 실행됩니다.

### 2. 프론트엔드 접속

브라우저에서 `http://localhost:3000` 접속

(백엔드가 정적 파일도 서빙합니다)

## 📡 API 엔드포인트

### GET /api/anime
전체 애니 목록 반환

**응답 예시:**
```json
[
  {
    "id": 1,
    "title": "장송의 프리렌",
    "tier": "SSS",
    "rating": 9.8,
    "oneLiner": "여운이 오래 남는 작품, 인생 애니 등극",
    "coverImage": "https://cdn.myanimelist.net/...",
    "featured": true,
    "category": ["인기", "추천"]
  }
]
```

### GET /api/anime/:id
단일 애니 상세 정보

### GET /api/categories
카테고리 목록

## 🗄️ DB 스키마

### anime 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER | PK |
| title | TEXT | 애니 제목 |
| tier | TEXT | SSS, SS, A, B, C, D, E |
| rating | REAL | 0.0 ~ 10.0 |
| one_liner | TEXT | 한줄평 |
| cover_image | TEXT | 포스터 URL |
| featured | INTEGER | Featured 여부 (0/1) |

### anime_categories 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER | PK |
| anime_id | INTEGER | FK → anime.id |
| category_name | TEXT | 카테고리명 |

## ⚠️ 주의사항

- HTML/CSS 레이아웃은 변경하지 않음
- 모든 데이터는 API를 통해서만 공급
- 카드 개수, 정렬, 크기는 기존 유지